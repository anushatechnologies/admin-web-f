export const EMAIL_PATTERNS = [
  /^[a-zA-Z0-9._%+-]+@credright\.com$/i,
  /^[a-zA-Z0-9._%+-]+@caspian\.in$/i,
];

export const REGION_KEYS = ['state_name', 'state_id', 'region_name', 'region_id', 'region'];

export const CLUSTER_KEYS = ['cluster_name', 'cluster_id', 'cluster'];

export const BRANCH_KEYS = ['branch_name', 'branch_id', 'branch'];

export const CLUSTER_AND_BRANCH_KEYS = [
  'cluster_name',
  'cluster_id',
  'cluster',
  'branch_name',
  'branch_id',
  'branch',
];
